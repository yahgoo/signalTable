name: signaltable-approval
version: 1.0.0
description: Intercept Telegram y/n/m (Version A shortlist) and YES/NO (registration) before the gateway agent runs.
author: SignalTable
hooks:
  - pre_gateway_dispatch
